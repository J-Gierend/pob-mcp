# .state — agent coordination runtime
.state/ = multi-console coordination runtime (agent presence, deploy gate, private journals, loop sidecars).
Everything here is gitignored except this README.
Spec: ~/.claude/docs/agent-state.md.
Never hand-edit another agent's files.
