# backlog

Idea lane (jg-v1 three-lane lifecycle: backlog → todo → changelog). Rows are dated one-liners
(`- YYYY-MM-DD — <idea>`) added only on Joshua's explicit approval — never on an agent's own
judgment. A row leaves only via org-plan (absorbed into a spec, row deleted) or deletion.

## Ideas

(none yet)
